{
  "name": "ai-crawler",
  "version": "0.1.0",
  "description": "Self-healing selector-based crawler prototype: LLM-authored schema.org JSON-LD + CSS selector map, kept in sync by a cheap periodic crawl, with LLM-assisted repair only on selector failure.",
  "main": "crawler.js",
  "scripts": {
    "crawl": "tsx --env-file=.env crawler.js",
    "dev": "tsx --env-file=.env index.js"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "tsx": "^4.23.1"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.10",
    "@sparticuz/chromium-min": "^147.0.0",
    "ai": "^7.0.28",
    "cheerio": "^1.1.2",
    "puppeteer": "^25.3.0",
    "puppeteer-core": "^25.3.0"
  },
  "license": "UNLICENSED"
}
